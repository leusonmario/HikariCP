
sudo: required
language: java

jdk:
  - openjdk8

script:
  - mvn clean compile assembly:single

before_deploy:
    - mkdir MiningBuild
    - find . -name '*.jar' -exec cp {} ./MiningBuild \;
    - cd /home/travis/build/leusonmario/HikariCP/MiningBuild
    - tar -zcvf result.tar.gz *
deploy:
  provider: releases
  api_key:
    secure: $GITHUB_TOKEN
  file: result.tar.gz
  name: fetchjar-4b225241fdad595cedfd5e97f5cc7f2df8d23577
  file_glob: true
  overwrite: true
  skip_cleanup: true
  on:
    all_branches: true 
            